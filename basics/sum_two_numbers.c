#include <stdio.h>
int main()
{
    int a,b,Sum;
    printf("Enter the value of a: ");
    scanf("%d",&a);
    printf("Enter the value of b: ");
    scanf("%d",&b);
    Sum = a+b;
    printf("The sum of the number a and b is %d",Sum);
    return 0;
}